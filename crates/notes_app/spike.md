# zednotes editor spike

This file is opened by `cargo run -p notes_app`.

`fo`
Markdown
jk
- **Markdown** syntax highlighting
- **Markdown** syntax highlighting
- Zed's native `Editor`
- Vim motions, operators, registers, repeat, and search
- `:w` or `Cmd+S` to save

```rust
fn editing_should_feel_instant() -> bool {
    true
}
```
